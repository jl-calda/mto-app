// Author-time consistency validation — pure checks over System/Model shapes.
// Surfaced (never blocking) in the wizard and model editor. Import from '@/lib/validate'.

export { validateSystem } from './system';
export { validateModel } from './model';
export { affected, byAffected } from './affected';
